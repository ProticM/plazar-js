{
  "name": "plazarjs",
  "private": true,
  "license": "MIT",
  "scripts": {
    "prepublishOnly": "gulp build && gulp build-bootstrap"
  },
  "dependencies": {
    "save-dev": "^2.0.0"
  },
  "devDependencies": {
    "gulp": "^4.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-fn": "0.0.3",
    "gulp-uglify": "^3.0.1",
    "lerna": "^3.4.3"
  }
}
